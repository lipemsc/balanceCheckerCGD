## balanceCheckerCGD

This is a simple tool to monitor your bank account from the terminal.

Very useful with the command `watch`.

#### Syntax

`python3 main.py [username] [password]`

If authentication data is omitted it will be asked interactively.
